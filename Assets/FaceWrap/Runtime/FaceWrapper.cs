using UnityEngine;

namespace FaceWrap.Runtime
{
    public class FaceWrapper : MonoBehaviour
    {
        // Start is called before the first frame update
        void Start()
        {
        
        }

        // Update is called once per frame
        void Update()
        {
        
        }
    }
}
